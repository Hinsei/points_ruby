function genCollectText(points) {
  return `Collection successfull you now have ${points} points`
}

document.getElementById("collect-container").innerHTML = 
  genCollectText(<%= @collection.collected_points %>)
